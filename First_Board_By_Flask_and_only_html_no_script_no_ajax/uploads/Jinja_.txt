1번파일
1. GET으로 인자받아서, JINJA2(HTML)에서 렌더링해서 보여주기
2. POST로 인자받아서, Jinja2에서 렌더링해서 보여주기(FORM하나 있어야됨)

2번파일
3. 파일 읽어서, Jinja2에서 렌더링해서 보여주기('/etc/passwd')

3번파일
4. Students 테이블에 학생 추가하는 로직 만들기(Form으로 입력받아서)
5. Students 테이블 리스트 보여주는 로직 만들기-> Table로 보여주기

4번파일
6. 사용자에게 GET방식으로 입력받은 URL을 iframe으로 보여주기

5번파일
7. 로그인 로그아웃 구현 -> Database와 같이, session도 같이, 이메일 받아서 보여주기
   -> 로그인할때 패스워드 입력할때 Casplock 켜져있는지 검사하기(JS)
8. 회원가입페이지 만들어서 유저 리스트도 보여주기

6번파일
9. 사용자에게 명령어를 입력받아서, 실행결과를 리턴해주는 페이지 만들기
   subprocess.Popen(['ls, '-al'])








